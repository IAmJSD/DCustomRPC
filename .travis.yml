language: python
cache: pip
matrix:
  include:
    - python: 3.6
      dist: trusty
    - python: 3.7
      dist: xenial
before_install:
  - pip install pycodestyle
script:
  - pycodestyle .
  - python -c "import yaml; yaml.load(open('config.yaml', 'r'))"
