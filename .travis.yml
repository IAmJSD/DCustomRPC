language: python
cache: pip
dist: xenial
python:
  - '3.6'
  - '3.7'
fail_fast: true
before_install:
  - pip install pycodestyle
script:
  - pycodestyle .
  - python -c "import yaml; yaml.load(open('config.yaml', 'r'))"
